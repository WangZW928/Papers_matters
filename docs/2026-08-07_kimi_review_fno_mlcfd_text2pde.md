按要求只给审查意见，不改文件。

**1) FNO 文档**
- 公式问题：
  - `G(a,θ)` / `G_θ` / `G^\dagger` 的符号在第 63-78 行和后文混用，前后层级不够统一，建议明确“参数化算子”与“真实算子”的对应关系，避免读者误以为 `G(a,θ)` 才是标准记号。
  - 第 151-155 行的频域乘法索引写法偏简化，`R_{k,l,j}` 的张量维度表达不够严谨，容易和论文里的 Fourier mode / channel 结构不一致。
  - 第 181-193 行 `Z_{k_max}` 的定义用 `or` 表达低频角块，语义上接近论文实现，但当前写法对多维频域集合的精确性不够，容易被理解成任意满足条件的并集。
- 代码/算法问题：
  - 第 197-203 行伪代码缺少对实数输入的共轭对称处理说明；如果按该伪代码直接实现，可能破坏输出的实值约束。
  - 第 205-206 行对分辨率不变性的解释过强，实际仍依赖采样、截断模态和训练分布覆盖，不能写得像“天然泛化”一样绝对。
- 格式问题：
  - 第 40-45 行有一行裸标题 `Fourier Neural Operator for Parametric Partial Differential Equations`，和前面的 `## 价值评估` 结构重复，版式不够整洁。
  - 第 245 行开始的仓库关联句子里，多个反引号文档名并列，风格上略显拥挤，但不算语法错误。
- 逻辑问题：
  - 第 44 行“已成为 Neural Operator 方向的基础论文”基本成立，但“计算结果强”与“proof-like”措辞偏满，建议收敛成“影响力大、基准表现突出”。
  - 第 237 行把主要成本概括为 FFT 还可以，但“性能瓶颈会转向 FFT 通信”这一句对单卡/多卡场景混写，结论需要分开表述。
- Review Questions：
  1. 如果 FNO 用于含复杂边界的 PDE，如何在保留 Fourier 低模态参数化的同时处理边界层和几何局部性？
  2. 对 Hamiltonian / incompressible 系统，FNO 的频域截断会优先破坏哪些守恒或拓扑量，如何加结构约束？
  3. 在多 GPU 3D FFT 场景下，FNO 的真实瓶颈更可能是通信还是复数矩阵乘？应如何建模？

**2) Machine Learning–Accelerated CFD 文档**
- 公式问题：
  - 第 71-80 行把能谱写成 `E(k)=1/2|u(k)|^2`，表述过于简化；若作为严格定义，通常还要说明谱密度与波数壳平均。
  - 第 99-116 行 Smagorinsky 项里 `|\overline{S}| = 2\sqrt{\sum \overline{S}_{ij}\overline{S}_{ij}}` 这一定义容易和常见应变率范数记号混淆，建议核对与原文的具体约定。
  - 第 202-211 行的成本模型 `T ~ (C_ML + C_physics)(N/K)^{d+1}` 过度理想化，把时间步、通信、投影解算等成本压缩得太粗。
- 代码/算法问题：
  - 第 149-167 行对 learned interpolation 的描述偏概念化，缺少说明网络输出是在每个 face 独立产生还是共享参数；实现层面不够清楚。
  - 第 169-177 行 learned correction 的公式写得像“后处理残差”，但论文中的训练与 rollout 过程更紧密，当前文字容易让人误解为简单 residual add-on。
  - 第 192 行提到 32 步 unroll，但没有说明梯度截断/checkpoint 具体如何影响稳定性与训练成本。
- 格式问题：
  - 第 43 行出现裸标题，和前文结构重复，建议删除或并入上一节。
  - 第 167 行内容过长，包含太多实现细节，排版可读性下降。
- 逻辑问题：
  - 第 16 行“每个空间维度细化 8–10 倍”与“40–80 倍加速”可以并列，但需要更明确地说明二者并非严格线性对应，取决于基线、网格和实现。
  - 第 263 行把本文与 FNO/PINN 的关系说得很顺，但“本文更偏 discretization correction”这一点是对的，建议避免把它概括为“learned solver”。
- Review Questions：
  1. learned interpolation 若迁移到 AMR coarse-fine 接口，如何保证守恒、一致性和长期稳定？
  2. 该方法主要修正对流通量近似；对高 Reynolds 数三维湍流，是否还需要显式建模亚格子应力？
  3. 在 GPU/TPU 上，如何把 stencil 网络、projection solve 和 AD 反传放进统一成本模型？

**3) Text2PDE 文档**
- 公式问题：
  - 第 105-153 行关于 kernel aggregation / mesh decoder 的写法过于“统一化”，把论文中的多组件流程压成一个连续积分公式，数学上可读但容易误导实现。
  - 第 217-229 行 DDPM 均值公式是标准形式，但与前文 `n` / `t` / `T` 的时间记号需要更明确区分，否则容易和 PDE 时间混淆。
  - 第 320-342 行 shape guidance 的能量项是推导性补写，不像论文主线公式，若不加说明，读者可能以为这是原文核心算法。
- 代码/算法问题：
  - 第 344 行已经提醒“主实验主要使用 vanilla conditional generation”，但第 280-342 行仍然给出较强的 CFG/梯度引导叙述，容易让人误以为这是论文默认主路径。
  - 第 363-374 行把全轨迹生成与 latent diffusion 关系讲清了，但没有点明推理时的采样步数与速度/质量权衡，这是实现关键。
  - 第 386-406 行的 HPC 成本式过于抽象，缺少 encoder/decoder 与 DiT 的实际占比分析。
- 格式问题：
  - 第 45-50 行重复标题与价值评估的结构仍偏冗余。
  - 第 408 行引用了多篇仓库文档，但部分文档名是否真实存在无法从当前文档内直接验证，建议避免写成确定性引用式表述。
- 逻辑问题：
  - 第 16 行“用户友好（自然语言驱动）带进神经 PDE 求解”成立，但“单一模型可直接依据文本描述和/或边界几何生成多样 PDE 解”语气偏强，建议补上“在数据集和条件集合覆盖范围内”。
  - 第 49 行“训练成本相当或更低”与“可用性导向”是合理总结，但需要注意文本条件本身在工程场景里仍然欠定，不能与物理约束同等看待。
- Review Questions：
  1. 文本条件在 PDE 场景下高度欠定，模型应如何输出不确定性而非单一 rollout？
  2. 对 Hamiltonian 或守恒系统，latent diffusion 如何避免破坏能量面、Casimir 或相位结构？
  3. 若部署到 HPC 流水线，主要瓶颈是 DiT 采样、mesh decoder 还是 guidance 反传，如何做步数与缓存优化？

我先停在这里等你下一步。